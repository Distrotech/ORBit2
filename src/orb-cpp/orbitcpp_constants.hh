namespace _orbitcpp {
    // header file version numbers
	int const MAJOR_VERSION = 0;
	int const MINOR_VERSION = 30;
	int const MICRO_VERSION = 2;

	// compiled in version numbers (used by configure script to check that the
	// library is the same version as the headers)
	extern int const LIB_MAJOR_VERSION;
	extern int const LIB_MINOR_VERSION;
	extern int const LIB_MICRO_VERSION;
};
